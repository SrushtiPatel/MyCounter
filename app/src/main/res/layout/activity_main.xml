<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btn_counter"
        android:layout_width="@dimen/dimen_120dp"
        android:layout_height="@dimen/dimen_120dp"
        android:text="@string/txt_tap"
        android:background="@drawable/circuler_ripple_selector"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:stateListAnimator="@drawable/button_statelist_selector"
        android:textColor="@color/white"
        tools:text="@string/txt_tap" />
    <TextView
        android:id="@+id/tv_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@id/btn_counter"
        app:layout_constraintLeft_toLeftOf="@id/btn_counter"
        android:layout_margin="@dimen/dimen_8dp"
        android:text="@string/txt_your_score"
        />
</android.support.constraint.ConstraintLayout>